{{ define "object_bucket_access" }}

resource "linode_object_storage_bucket" "foobar" {
    region = "{{.Region}}"
    label = "{{.Label}}"

    acl = "{{.ACL}}"
    cors_enabled = {{.CORSEnabled}}
}

{{ end }}