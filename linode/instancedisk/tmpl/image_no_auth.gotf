{{ define "instance_disk_image_no_auth" }}

resource "linode_instance" "foobar" {
    label = "{{ .Label }}"
    type = "g6-nanode-1"
    region = "{{ .Region }}"
}

resource "linode_instance_disk" "foobar" {
    label = "{{ .Label }}"
    linode_id = linode_instance.foobar.id
    size = {{ .Size }}

    image = "linode/debian12"
}

{{ end }}

