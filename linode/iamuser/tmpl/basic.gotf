{{ define "iam_user_basic" }}

resource "linode_user" "test_user" {
    username = "{{.Username}}"
    email = "{{.Email}}"
    restricted = {{.Restricted}}
}

resource "linode_volume" "test_volume" {
    label = "{{.VolumeLabel}}"
    region = "{{.Region}}"
    tags = ["tf_test"]
}

{{ end }}
