{{ define "vpcs_data_filter_label" }}

resource "linode_vpc" "foo" {
    label = "{{.Label}}"
    region = "{{.Region}}"
}

data "linode_vpcs" "foobar" {
    filter {
        name = "label"
        values = ["tf-test"]
        match_by = "substring"
    }
}

{{ end }}