{{ define "vpc_subnet_basic" }}

provider "linode" {
    url = "https://api.dev.linode.com"
    api_version = "v4beta"
}

resource "linode_vpc_subnet" "foobar" {
    vpc_id = "{{.VPCId}}"
    label = "{{.Label}}"
    ipv4 = "{{.IPv4}}"
}

{{ end }}