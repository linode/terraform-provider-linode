{{ define "monitor_logs_stream_invalid_destination" }}

resource "linode_monitor_logs_stream" "foobar" {
  label        = "{{.Label}}"
  type         = "audit_logs"
  destinations = [999999999]
}

{{ end }}
