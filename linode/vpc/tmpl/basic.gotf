{{ define "vpc_basic" }}

provider "linode" {
    url = "https://api.dev.linode.com"
    api_version = "v4beta"
}

resource "linode_vpc" "foobar" {
    label = "{{.Label}}"
    region = "{{.Region}}"
    description = "some description"
}

{{ end }}